<div class="user_info">
    <a href="<%= user_path(current_user) %>">
      <%= gravatar_for current_user %>
    <span class="user_name">
      <p><%= current_user.name %></p>
    </span></a>
  <%= render='shared/stats' %>
</div>
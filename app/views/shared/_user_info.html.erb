<div class="user_info">
    <a href="<%= user_path(current_user) %>">
    <%= gravatar_for current_user %>
    <span class="user_name">
        <%= current_user.name %>
    </span>
    <span class="fails">
        <%= pluralize(current_user.fails.count, "fail") %>
    </span>
    </a>
</div>
<div id="comments">
  <% @comments.each do |comment| %>
    <div class="comment">
      <%= markdown comment.content %>
      <strong>Commented <%= time_ago_in_words(comment.created_at) %> ago.</strong><br>
    </div>
  <% end %>
</div>